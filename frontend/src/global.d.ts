// Project-wide global type declarations for the frontend

// Vite define replacement used in the app (declared as readonly so code can
// read it without TypeScript complaining). Adjust type as appropriate.
declare const __API_BASE_URL__: string;

export {};
